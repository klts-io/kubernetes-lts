name: Verify

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

  workflow_dispatch:

jobs:
  Patch:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Verify patch
        run: |
          make verify-patch
      - name: Verify patch format
        run: |
          make verify-patch-format

  Select-Releases:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      run_all: ${{ steps.select.outputs.run_all }}
      selected_releases: ${{ steps.select.outputs.selected_releases }}
    steps:
      - id: select
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          HEAD_REF: ${{ github.head_ref }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          RELEASES: ',v1.32.13-lts.1,v1.31.14-lts.1,v1.30.14-lts.1,v1.29.15-lts.1,v1.28.15-lts.2,v1.28.15-lts.0,v1.27.16-lts.1,v1.26.15-lts.1,v1.25.16-lts.1,v1.24.17-lts.1,v1.23.17-lts.1,v1.22.17-lts.1,v1.21.14-lts.2,v1.20.15-lts.3,v1.19.16-lts.4,v1.18.20-lts.3,v1.17.17-lts.3,v1.16.15-lts.3,'
        run: |
          set -o errexit
          set -o nounset
          set -o pipefail

          selected_releases=""
          run_all="false"

          if [[ "${EVENT_NAME}" != "pull_request" ]]; then
            run_all="true"
            selected_releases="${RELEASES}"
          else
            selector="$(echo "${HEAD_REF} ${PR_TITLE}" | tr '[:upper:]' '[:lower:]')"
            IFS=',' read -r -a release_list <<< "${RELEASES}"
            for release in "${release_list[@]}"; do
              if [[ -z "${release}" ]]; then
                continue
              fi
              release_lower="$(echo "${release}" | tr '[:upper:]' '[:lower:]')"
              minor_dot="$(echo "${release}" | sed -E 's/^v([0-9]+)\.([0-9]+)\..*/\1.\2/')"
              minor_dash="${minor_dot/./-}"

              if [[ "${selector}" == *"${release_lower}"* ]] || [[ "${selector}" == *"${minor_dot}"* ]] || [[ "${selector}" == *"${minor_dash}"* ]]; then
                if [[ -z "${selected_releases}" ]]; then
                  selected_releases=",${release},"
                else
                  selected_releases="${selected_releases}${release},"
                fi
              fi
            done

            if [[ -z "${selected_releases}" ]]; then
              run_all="true"
              selected_releases="${RELEASES}"
            fi
          fi

          echo "run_all=${run_all}" >> "${GITHUB_OUTPUT}"
          echo "selected_releases=${selected_releases}" >> "${GITHUB_OUTPUT}"
          echo "Selected releases: ${selected_releases}"

  Test-v1-32-13-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.32.13-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-32-13-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.32.13-lts.1
        run: |
          make v1.32.13-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test
        run: |
          make test

  Test-Cmd-v1-32-13-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.32.13-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-32-13-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.32.13-lts.1
        run: |
          make v1.32.13-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test cmd
        run: |
          make test-cmd

  Test-Integration-v1-32-13-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.32.13-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
            /tmp/kubernetes-lts/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-32-13-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.32.13-lts.1
        run: |
          make v1.32.13-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test integration
        run: |
          make test-integration

  Test-v1-31-14-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.31.14-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-31-14-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.31.14-lts.1
        run: |
          make v1.31.14-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test
        run: |
          make test

  Test-Cmd-v1-31-14-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.31.14-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-31-14-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.31.14-lts.1
        run: |
          make v1.31.14-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test cmd
        run: |
          make test-cmd

  Test-Integration-v1-31-14-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.31.14-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
            /tmp/kubernetes-lts/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-31-14-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.31.14-lts.1
        run: |
          make v1.31.14-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test integration
        run: |
          make test-integration

  Test-v1-30-14-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.30.14-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-30-14-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.30.14-lts.1
        run: |
          make v1.30.14-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test
        run: |
          make test

  Test-Cmd-v1-30-14-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.30.14-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-30-14-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.30.14-lts.1
        run: |
          make v1.30.14-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test cmd
        run: |
          make test-cmd

  Test-Integration-v1-30-14-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.30.14-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
            /tmp/kubernetes-lts/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-30-14-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.30.14-lts.1
        run: |
          make v1.30.14-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test integration
        run: |
          make test-integration

  Test-v1-29-15-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.29.15-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-29-15-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.29.15-lts.1
        run: |
          make v1.29.15-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test
        run: |
          make test

  Test-Cmd-v1-29-15-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.29.15-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-29-15-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.29.15-lts.1
        run: |
          make v1.29.15-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test cmd
        run: |
          make test-cmd

  Test-Integration-v1-29-15-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.29.15-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
            /tmp/kubernetes-lts/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-29-15-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.29.15-lts.1
        run: |
          make v1.29.15-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test integration
        run: |
          make test-integration

  Test-v1-28-15-lts-2:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.28.15-lts.2,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-28-15-lts-2
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.28.15-lts.2
        run: |
          make v1.28.15-lts.2
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test
        run: |
          make test

  Test-Cmd-v1-28-15-lts-2:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.28.15-lts.2,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-28-15-lts-2
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.28.15-lts.2
        run: |
          make v1.28.15-lts.2
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test cmd
        run: |
          make test-cmd

  Test-Integration-v1-28-15-lts-2:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.28.15-lts.2,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
            /tmp/kubernetes-lts/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-28-15-lts-2
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.28.15-lts.2
        run: |
          make v1.28.15-lts.2
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test integration
        run: |
          make test-integration

  Test-v1-28-15-lts-0:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.28.15-lts.0,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-28-15-lts-0
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.28.15-lts.0
        run: |
          make v1.28.15-lts.0
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test
        run: |
          make test

  Test-Cmd-v1-28-15-lts-0:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.28.15-lts.0,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-28-15-lts-0
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.28.15-lts.0
        run: |
          make v1.28.15-lts.0
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test cmd
        run: |
          make test-cmd

  Test-Integration-v1-28-15-lts-0:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.28.15-lts.0,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
            /tmp/kubernetes-lts/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-28-15-lts-0
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.28.15-lts.0
        run: |
          make v1.28.15-lts.0
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test integration
        run: |
          make test-integration

  Test-v1-27-16-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.27.16-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-27-16-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.27.16-lts.1
        run: |
          make v1.27.16-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test
        run: |
          make test

  Test-Cmd-v1-27-16-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.27.16-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-27-16-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.27.16-lts.1
        run: |
          make v1.27.16-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test cmd
        run: |
          make test-cmd

  Test-Integration-v1-27-16-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.27.16-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
            /tmp/kubernetes-lts/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-27-16-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.27.16-lts.1
        run: |
          make v1.27.16-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test integration
        run: |
          make test-integration

  Test-v1-26-15-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.26.15-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-26-15-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.26.15-lts.1
        run: |
          make v1.26.15-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test
        run: |
          make test

  Test-Cmd-v1-26-15-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.26.15-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-26-15-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.26.15-lts.1
        run: |
          make v1.26.15-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test cmd
        run: |
          make test-cmd

  Test-Integration-v1-26-15-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.26.15-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
            /tmp/kubernetes-lts/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-26-15-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.26.15-lts.1
        run: |
          make v1.26.15-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test integration
        run: |
          make test-integration

  Test-v1-25-16-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.25.16-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-25-16-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.25.16-lts.1
        run: |
          make v1.25.16-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test
        run: |
          make test

  Test-Cmd-v1-25-16-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.25.16-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-25-16-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.25.16-lts.1
        run: |
          make v1.25.16-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test cmd
        run: |
          make test-cmd

  Test-Integration-v1-25-16-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.25.16-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
            /tmp/kubernetes-lts/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-25-16-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.25.16-lts.1
        run: |
          make v1.25.16-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test integration
        run: |
          make test-integration

  Test-v1-24-17-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.24.17-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-24-17-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.24.17-lts.1
        run: |
          make v1.24.17-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test
        run: |
          make test

  Test-Cmd-v1-24-17-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.24.17-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-24-17-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.24.17-lts.1
        run: |
          make v1.24.17-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test cmd
        run: |
          make test-cmd

  Test-Integration-v1-24-17-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.24.17-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
            /tmp/kubernetes-lts/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-24-17-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.24.17-lts.1
        run: |
          make v1.24.17-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test integration
        run: |
          make test-integration

  Test-v1-23-17-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.23.17-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-23-17-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.23.17-lts.1
        run: |
          make v1.23.17-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test
        run: |
          make test

  Test-Cmd-v1-23-17-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.23.17-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-23-17-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.23.17-lts.1
        run: |
          make v1.23.17-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test cmd
        run: |
          make test-cmd

  Test-Integration-v1-23-17-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.23.17-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
            /tmp/kubernetes-lts/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-23-17-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.23.17-lts.1
        run: |
          make v1.23.17-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test integration
        run: |
          make test-integration

  Test-v1-22-17-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.22.17-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-22-17-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.22.17-lts.1
        run: |
          make v1.22.17-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test
        run: |
          make test

  Test-Cmd-v1-22-17-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.22.17-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-22-17-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.22.17-lts.1
        run: |
          make v1.22.17-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test cmd
        run: |
          make test-cmd

  Test-Integration-v1-22-17-lts-1:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.22.17-lts.1,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
            /tmp/kubernetes-lts/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-22-17-lts-1
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.22.17-lts.1
        run: |
          make v1.22.17-lts.1
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test integration
        run: |
          make test-integration

  Test-v1-21-14-lts-2:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.21.14-lts.2,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-21-14-lts-2
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.21.14-lts.2
        run: |
          make v1.21.14-lts.2
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test
        run: |
          make test

  Test-Cmd-v1-21-14-lts-2:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.21.14-lts.2,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-21-14-lts-2
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.21.14-lts.2
        run: |
          make v1.21.14-lts.2
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test cmd
        run: |
          make test-cmd

  Test-Integration-v1-21-14-lts-2:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.21.14-lts.2,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
            /tmp/kubernetes-lts/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-21-14-lts-2
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.21.14-lts.2
        run: |
          make v1.21.14-lts.2
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test integration
        run: |
          make test-integration

  Test-v1-20-15-lts-3:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.20.15-lts.3,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-20-15-lts-3
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.20.15-lts.3
        run: |
          make v1.20.15-lts.3
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test
        run: |
          make test

  Test-Cmd-v1-20-15-lts-3:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.20.15-lts.3,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-20-15-lts-3
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.20.15-lts.3
        run: |
          make v1.20.15-lts.3
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test cmd
        run: |
          make test-cmd

  Test-Integration-v1-20-15-lts-3:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.20.15-lts.3,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
            /tmp/kubernetes-lts/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-20-15-lts-3
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.20.15-lts.3
        run: |
          make v1.20.15-lts.3
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test integration
        run: |
          make test-integration

  Test-v1-19-16-lts-4:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.19.16-lts.4,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-19-16-lts-4
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.19.16-lts.4
        run: |
          make v1.19.16-lts.4
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test
        run: |
          make test

  Test-Cmd-v1-19-16-lts-4:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.19.16-lts.4,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-19-16-lts-4
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.19.16-lts.4
        run: |
          make v1.19.16-lts.4
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test cmd
        run: |
          make test-cmd

  Test-Integration-v1-19-16-lts-4:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.19.16-lts.4,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
            /tmp/kubernetes-lts/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-19-16-lts-4
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.19.16-lts.4
        run: |
          make v1.19.16-lts.4
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test integration
        run: |
          make test-integration

  Test-v1-18-20-lts-3:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.18.20-lts.3,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-18-20-lts-3
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.18.20-lts.3
        run: |
          make v1.18.20-lts.3
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test
        run: |
          make test

  Test-Cmd-v1-18-20-lts-3:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.18.20-lts.3,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-18-20-lts-3
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.18.20-lts.3
        run: |
          make v1.18.20-lts.3
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test cmd
        run: |
          make test-cmd

  Test-Integration-v1-18-20-lts-3:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.18.20-lts.3,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
            /tmp/kubernetes-lts/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-18-20-lts-3
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.18.20-lts.3
        run: |
          make v1.18.20-lts.3
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test integration
        run: |
          make test-integration

  Test-v1-17-17-lts-3:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.17.17-lts.3,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-17-17-lts-3
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.17.17-lts.3
        run: |
          make v1.17.17-lts.3
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test
        run: |
          make test

  Test-Cmd-v1-17-17-lts-3:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.17.17-lts.3,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-17-17-lts-3
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.17.17-lts.3
        run: |
          make v1.17.17-lts.3
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test cmd
        run: |
          make test-cmd

  Test-Integration-v1-17-17-lts-3:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.17.17-lts.3,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
            /tmp/kubernetes-lts/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-17-17-lts-3
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.17.17-lts.3
        run: |
          make v1.17.17-lts.3
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test integration
        run: |
          make test-integration

  Test-v1-16-15-lts-3:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.16.15-lts.3,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-16-15-lts-3
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.16.15-lts.3
        run: |
          make v1.16.15-lts.3
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test
        run: |
          make test

  Test-Cmd-v1-16-15-lts-3:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.16.15-lts.3,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-16-15-lts-3
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.16.15-lts.3
        run: |
          make v1.16.15-lts.3
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test cmd
        run: |
          make test-cmd

  Test-Integration-v1-16-15-lts-3:
    needs:
      - Patch
      - Select-Releases
    if: ${{ needs.Select-Releases.outputs.run_all == 'true' || contains(needs.Select-Releases.outputs.selected_releases, ',v1.16.15-lts.3,') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        env:
          cache-name: src
        with:
          path: |
            src
            /tmp/kubernetes-lts/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-v1-16-15-lts-3
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - name: Config Github
        run: |
          git config --global user.email "noreply@github.com"
          git config --global user.name "GitHub"
      - name: Install dependent
        run: |
          make dependent
      - name: Checkout to v1.16.15-lts.3
        run: |
          make v1.16.15-lts.3
      - name: Install etcd
        run: |
          make install-etcd
      - name: Test integration
        run: |
          make test-integration

